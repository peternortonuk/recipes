"""
https://realpython.com/python-itertools/
An iterable object can return elements one at a time. 
Any Python object that implements the .__iter__() or .__getitem__() methods is iterable.

The iter() built-in function, when called on an iterable, returns an iterator object for that iterable:
"""
